# Copy this file to `.env` and fill in real values. `.env` is git-ignored.

# "cpu", "cuda", "cuda:0", or "mps" (Apple Silicon)
PPE_DEVICE=cpu

PPE_LOG_LEVEL=INFO

# Telegram alerts (Milestone 9) — leave blank to disable alerts for now.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
